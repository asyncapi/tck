asyncapi: 3.0.0
info:
  title: Valid Operation Object
  version: 1.0.0
channels:
  userSignup:
    address: users/signup
operations:
  processSignup:
    action: receive
    channel:
      $ref: '#/channels/userSignup'
    summary: Process user signup
