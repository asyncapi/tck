asyncapi: 3.0.0

info:
  title: Channel Test
  version: 1.0.0

channels:
  user/signup:
    address: user/signup
    messages:
      UserSignedUp:
        payload:
          type: object
          properties:
            userId:
              type: string
            timestamp:
              type: string
              format: date-time
