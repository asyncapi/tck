asyncapi: 3.0.0
info:
  title: Valid Channel Object
  version: 1.0.0
channels:
  userSignup:
    address: users/signup
    messages:
      userSignedUp:
        payload:
          type: string
    title: User Signup Channel
    description: This channel is used to sign up users.
