asyncapi: 3.0.0

info:
  title: Invalid Operations Test
  version: 1.0.0

channels:
  user/signup:
    address: user/signup
    messages:
      UserSignedUp:
        payload:
          type: object

operations:
  onUserSignup:
    # Missing required 'action' field
    channel:
      $ref: '#/channels/user~1signup'
