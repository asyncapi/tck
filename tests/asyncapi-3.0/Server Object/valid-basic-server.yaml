asyncapi: 3.0.0

info:
  title: Server Test
  version: 1.0.0

servers:
  production:
    host: api.example.com
    protocol: kafka
    description: Production Kafka broker
  development:
    host: localhost:9092
    protocol: kafka
    description: Development Kafka broker

channels:
  user/events:
    address: user/events
    messages:
      UserEvent:
        payload:
          type: object
