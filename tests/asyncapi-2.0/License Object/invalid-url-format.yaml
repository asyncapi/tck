asyncapi: 2.0.0

info:
  title: AsyncAPI Sample App
  version: 1.0.1
  license:
    name: Apache 2.0
    url: this is not in the format of a URL

channels:
  /user/signedup:
    subscribe:
      message:
        payload:
          type: object
          properties:
            email:
              type: string
              format: email
