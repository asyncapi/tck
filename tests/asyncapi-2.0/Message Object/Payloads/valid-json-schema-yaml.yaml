asyncapi: 2.0.0

info:
  title: Signup service example (internal)
  version: 0.1.0

channels:
  /user/signedup:
    subscribe:
      message:
        schemaFormat: application/schema+yaml;version=draft-07
        payload:
          $ref: includes/user-json-schema-optional.yaml
