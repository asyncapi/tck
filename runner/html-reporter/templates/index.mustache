<!DOCTYPE html>
<html lang="en">
<head>
  <title>AsyncAPI Parsers Overview</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="./bootstrap.min.css">
  <link rel="stylesheet" href="./custom.css">
</head>
  <body>
    <div class="container-fluid">
      <h3>Overview</h3>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Parser</th>
            <th>Valid Files <span class="stat-nums">(passed/all)</span></th>
            <th>Invalid Files <span class="stat-nums">(passed/all)</span></th>
            <th>Total <span class="stat-nums">(passed/all)</span></th>
            <th colspan="2"></th>
          </tr>
        </thead>
        <tbody>
          {{#stats}}
            <tr>
              <td>
                <a href="{{{parser.url}}}" target="blank" title="Visit parser's web-page">
                  {{parser.name}} ({{parser.language}}, {{parser.version}})
                </a>
              </td>
              <td>
                {{valid.successPerc}}%
                <span class="stat-nums">({{valid.success}}/{{valid.total}})</span>
              </td>
              <td>
                {{invalid.successPerc}}%
                <span class="stat-nums">({{invalid.success}}/{{invalid.total}})</span></td>
              <td>
                {{all.successPerc}}%
                <span class="stat-nums">({{all.success}}/{{all.total}})</span></td>
              <td><a href="{{parser.name}}_detailed_report.html" title="View {{parser.name}} detailed report">Detailed report</a></td>
              <td><a href="{{parser.name}}_features_stats.html" title="View {{parser.name}} features stats">Features stats</a></td>
            </tr>
          {{/stats}}
        </tbody>
      </table>
    </div>
  </body>
</html>
